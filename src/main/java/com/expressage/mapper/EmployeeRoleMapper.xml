<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.expressage.mapper.EmployeeRoleMapper" >
  <resultMap id="BaseResultMap" type="com.expressage.pojo.EmployeeRole" >
    <result column="eid" property="eid" jdbcType="INTEGER" />
    <result column="rid" property="rid" jdbcType="INTEGER" />
  </resultMap>
  <insert id="insert" parameterType="com.expressage.pojo.EmployeeRole" >
    insert into employee_role (eid, rid)
    values (#{eid,jdbcType=INTEGER}, #{rid,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.expressage.pojo.EmployeeRole" >
    insert into employee_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="eid != null" >
        eid,
      </if>
      <if test="rid != null" >
        rid,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="eid != null" >
        #{eid,jdbcType=INTEGER},
      </if>
      <if test="rid != null" >
        #{rid,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <insert id="zkAddRoleByEid">
  	INSERT INTO expressage.employee_role (eid, rid)VALUES(#{eid}, #{rid});
  </insert>
  <delete id="zkDelRoleByEid">
  	delete from employee_role where eid = #{eid}
  </delete>
  <select id="zkEmplCountByRid" resultType="int">
  	SELECT COUNT(1) FROM employee_role WHERE rid = #{rid}
  </select>
</mapper>